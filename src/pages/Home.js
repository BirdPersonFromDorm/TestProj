import React from 'react';
import MainInfo from "../components/MainInfo";

function Home(props) {
    return (
        <div className="wrapper">
            <MainInfo/>
        </div>
    );
}

export default Home;
